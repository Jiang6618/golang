package test

import "testing"

/*
	GO中if语句和其他语言中的类似, 根据给定的条件表达式运算结果来判断执行流程
	1. if中布尔表达式不用使用括号
	2. 初始变量可以声明在布尔表达式里, 注意它的作用域
	3. 不能使用0或非0 表示真假
	4. 大括号{} 必须存在, 即使只有一行语句
	5. 左括号必须在if 或 else 的同一行
	6. 在if之后, 条件语句之前, 可以添加变量初始化语句, 使用 ; 进行分隔
*/

func TestIF(t *testing.T) {

}
